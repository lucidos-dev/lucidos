# Documentation site dependencies (mkdocs-material + transclusion plugin).
# Used by `mkdocs build --strict` locally, and by the workspace trigger
# "Publish lucidos.dev docs", which installs from here into a dedicated venv
# before building and deploying. Nothing in CI reads it: the docs deploy runs on
# the maintainer's machine, never from public CI (docs/adr/0031).
# Pinned for reproducible builds; bump deliberately.
mkdocs-material==9.7.7
mkdocs-include-markdown-plugin==7.3.0
