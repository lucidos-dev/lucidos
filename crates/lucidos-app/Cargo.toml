[package]
name = "lucidos-app"
version = "0.1.0"
edition = "2021"

[dependencies]
tauri = { version = "2", features = ["unstable"] }
tauri-plugin-window-state = "2"
serde_json = "1"

[build-dependencies]
tauri-build = { version = "2", features = [] }
